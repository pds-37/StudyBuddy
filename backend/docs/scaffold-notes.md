# Scaffold Notes

This repository was reset from the earlier product direction and is now scaffolded for:

- a React + Tailwind + React Router frontend
- an Express + MongoDB backend
- shared TypeScript contracts
- free-tier AI provider integration points

The current codebase contains placeholders only so Step 3 can be designed cleanly without mixed implementation details.
